const { Component, Mixin } = Shopware;
import template from './weedesign-images2webp-media-options.html.twig';

Component.register('weedesign-images2webp-media-options', {
    template,
    props: ['label']
});