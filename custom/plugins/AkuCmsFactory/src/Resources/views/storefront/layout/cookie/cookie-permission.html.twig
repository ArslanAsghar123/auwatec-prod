{% sw_extends '@Storefront/storefront/layout/cookie/cookie-permission.html.twig' %}

{% block layout_cookie_permission_inner %}
{# acceptAllCookies wird zur Zeit leider nicht ans Frontend übergeben #}
{% set shopware = shopware|merge({
        config: {
            core: {
                basicInformation: {
                    acceptAllCookies: true
                }
            }
        }
    }) %}
    {{parent()}}

	
{% endblock %}